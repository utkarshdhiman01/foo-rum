import { Outlet } from "react-router";
import Header from "../../components/Header";

const Layout = () => {
  return (
    <>
      <Header />
      iwuherioweh
      <main>
        <Outlet />
      </main>
    </>
  );
};

export default Layout;
